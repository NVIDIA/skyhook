# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# yaml-language-server: $schema=https://raw.githubusercontent.com/kyverno/chainsaw/main/.schemas/json/test-chainsaw-v1alpha1.json
apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: uninstall-on-delete
  labels:
    pool: uninstall
spec:
  timeouts:
    assert: 240s
  catch:
    - get:
        apiVersion: v1
        kind: Node
        selector: nodewright.nvidia.com/test-node=skyhooke2e
        format: yaml
    - get:
        apiVersion: nodewright.nvidia.com/v1alpha1
        kind: NodeWright
        name: uninstall-on-delete
        format: yaml
  steps:
  - name: install
    description: >
      Create skyhook with one uninstall-enabled and one disabled package.
      Verify both install successfully.
    try:
    - script:
        content: |
          ../nodewright-cli reset uninstall-on-delete --confirm 2>/dev/null || true
    - create:
        file: nodewright.yaml
    - assert:
        resource:
          apiVersion: nodewright.nvidia.com/v1alpha1
          kind: NodeWright
          metadata:
            name: uninstall-on-delete
          status:
            status: complete

  - name: delete-cr
    description: >
      Delete the Skyhook CR. The finalizer should trigger uninstall pods only for
      the enabled package. The CR should eventually be fully deleted.
    try:
    - script:
        content: |
          kubectl delete nodewright uninstall-on-delete --wait=false
    - assert:
        ## uninstall pod for enabled package
        resource:
          kind: Pod
          apiVersion: v1
          metadata:
            namespace: nodewright
            labels:
              nodewright.nvidia.com/name: uninstall-on-delete
              nodewright.nvidia.com/package: enabled-pkg-2.1.4
          spec:
            initContainers:
              - name: enabled-pkg-init
              - name: enabled-pkg-uninstall
                args:
                  ([0]): uninstall
              - name: enabled-pkg-uninstallcheck
                args:
                  ([0]): uninstall-check
    - script:
        ## verify the CR is eventually deleted
        content: |
          for i in $(seq 1 60); do
            if ! kubectl get nodewright uninstall-on-delete 2>/dev/null; then
              echo "CR deleted successfully"
              exit 0
            fi
            sleep 3
          done
          echo "CR still exists after timeout"
          exit 1
        timeout: 240s
    - assert:
        ## After the CR is gone, disabled-pkg (uninstall.enabled unset/false) must
        ## still be present in the node state annotation. Phase 3 cleanup removes
        ## the status annotation and labels, but preserves nodeState when it
        ## still records packages that were not uninstalled — D2 semantics:
        ## non-absent entry means files remain on host.
        resource:
          apiVersion: v1
          kind: Node
          metadata:
            labels:
              nodewright.nvidia.com/test-node: skyhooke2e
            annotations:
              (parse_json("nodewright.nvidia.com/nodeState_uninstall-on-delete")."disabled-pkg|2.1.4".name == 'disabled-pkg'): true
              (parse_json("nodewright.nvidia.com/nodeState_uninstall-on-delete")."disabled-pkg|2.1.4".stage == 'config'): true
              (parse_json("nodewright.nvidia.com/nodeState_uninstall-on-delete")."disabled-pkg|2.1.4".state == 'complete'): true
