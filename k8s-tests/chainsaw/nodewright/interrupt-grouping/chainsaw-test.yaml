# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# yaml-language-server: $schema=https://raw.githubusercontent.com/kyverno/chainsaw/main/.schemas/json/test-chainsaw-v1alpha1.json
apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: interrupt-grouping
  labels:
    pool: interrupt
spec:
  catch:
      ## if errors, print the most important info
    - get:
        apiVersion: v1
        kind: Node
        selector: nodewright.nvidia.com/test-node=skyhooke2e
        format: yaml
    - get:
        apiVersion: nodewright.nvidia.com/v1alpha1
        kind: NodeWright
        name: interrupt-grouping
        format: yaml
  timeouts:
    assert: 180s ## needs buffer since it fights with the interrupt test for cordoning nodes
  steps:
  - name: grouping
    description: Create an interrupt skyhook with group labels and verify nodes are processed respecting group constraints
    try:
    - script:
        content: |
          ## remove annotation from last run
          ../nodewright-cli reset interrupt-grouping --confirm 2>/dev/null || true
    - create:
        file: nodewright.yaml
    ## sequential: dax pod transitions through apply → config → interrupt → post-interrupt stages
    - assert:
        ## node in_progress, cordoned
        resource:
          kind: Node
          apiVersion: v1
          metadata:
            labels:
              nodewright.nvidia.com/test-node: skyhooke2e
              nodewright.nvidia.com/status_interrupt-grouping: in_progress
            annotations:
              nodewright.nvidia.com/status_interrupt-grouping: in_progress
          spec:
            taints:
            - effect: NoSchedule
              key: node.kubernetes.io/unschedulable
          status:
            (conditions[?type == 'nodewright.nvidia.com/interrupt-grouping/NotReady']):
            - reason: "Incomplete"
              status: "True"
            (conditions[?type == 'nodewright.nvidia.com/interrupt-grouping/Erroring']):
            - reason: "Not Erroring"
              status: "False"
    - assert:
        ## dax apply pod
        resource:
          kind: Pod
          apiVersion: v1
          metadata:
            namespace: nodewright
            labels:
              nodewright.nvidia.com/name: interrupt-grouping
              nodewright.nvidia.com/package: dax-1.2.3
            annotations:
              nodewright.nvidia.com/package: >-
                {"name":"dax","version":"1.2.3","skyhook":"interrupt-grouping","stage":"apply","image":"ghcr.io/nvidia/skyhook/agentless"}
            ownerReferences:
              ## controller: true proves the pod is the Job's child, not merely referencing
              ## one. The Job name carries a config hash and is deliberately not asserted.
              - apiVersion: batch/v1
                kind: Job
                controller: true
          spec:
            initContainers:
              - name: dax-init
                image: ghcr.io/nvidia/skyhook/agentless:1.2.3
              - name: dax-apply
                image: ghcr.io/nvidia/skyhook/agentless:6.2.0
                args:
                  ([0]): apply
                  ([1]): /root
                  (length(@)): 3
              - name: dax-applycheck
                image: ghcr.io/nvidia/skyhook/agentless:6.2.0
                args:
                  ([0]): apply-check
                  ([1]): /root
                  (length(@)): 3
            (tolerations[?key == 'nvidia.com/gpu']):
              - operator: Exists
    - assert:
        ## dax config pod
        resource:
          kind: Pod
          apiVersion: v1
          metadata:
            namespace: nodewright
            labels:
              nodewright.nvidia.com/name: interrupt-grouping
              nodewright.nvidia.com/package: dax-1.2.3
            annotations:
              nodewright.nvidia.com/package: >-
                {"name":"dax","version":"1.2.3","skyhook":"interrupt-grouping","stage":"config","image":"ghcr.io/nvidia/skyhook/agentless"}
            ownerReferences:
              ## controller: true proves the pod is the Job's child, not merely referencing
              ## one. The Job name carries a config hash and is deliberately not asserted.
              - apiVersion: batch/v1
                kind: Job
                controller: true
          spec:
            initContainers:
              - name: dax-init
              - name: dax-config
                args:
                  ([0]): config
                  ([1]): /root
                  (length(@)): 3
              - name: dax-configcheck
                args:
                  ([0]): config-check
                  ([1]): /root
                  (length(@)): 3
            (tolerations[?key == 'nvidia.com/gpu']):
              - operator: Exists
    - assert:
        ## dax interrupt pod
        resource:
          kind: Pod
          apiVersion: v1
          metadata:
            namespace: nodewright
            labels:
              nodewright.nvidia.com/name: interrupt-grouping
              nodewright.nvidia.com/package: dax-1.2.3
            annotations:
              ("nodewright.nvidia.com/package" && parse_json("nodewright.nvidia.com/package")):
                {
                  "name": "dax",
                  "version": "1.2.3",
                  "skyhook": "interrupt-grouping",
                  "stage": "interrupt",
                  "image": "ghcr.io/nvidia/skyhook/agentless"
                }
            ownerReferences:
              ## controller: true proves the pod is the Job's child, not merely referencing
              ## one. The Job name carries a config hash and is deliberately not asserted.
              - apiVersion: batch/v1
                kind: Job
                controller: true
          spec:
            initContainers:
              - name: interrupt
                args:
                  ([0]): interrupt
                  ([1]): /root
                  (parse_json(base64_decode([3]))):
                    {
                        "type": "service_restart",
                        "services": [
                            "containerd",
                            "cron",
                            "foobar"
                        ]
                    }
                  (length(@)): 4
            (tolerations[?key == 'nvidia.com/gpu']):
              - operator: Exists
    - assert:
        ## dax post-interrupt pod
        resource:
          kind: Pod
          apiVersion: v1
          metadata:
            namespace: nodewright
            labels:
              nodewright.nvidia.com/name: interrupt-grouping
              nodewright.nvidia.com/package: dax-1.2.3
            annotations:
              nodewright.nvidia.com/package: >-
                {"name":"dax","version":"1.2.3","skyhook":"interrupt-grouping","stage":"post-interrupt","image":"ghcr.io/nvidia/skyhook/agentless"}
            ownerReferences:
              ## controller: true proves the pod is the Job's child, not merely referencing
              ## one. The Job name carries a config hash and is deliberately not asserted.
              - apiVersion: batch/v1
                kind: Job
                controller: true
          spec:
            initContainers:
              - name: dax-init
              - name: dax-post-interrupt
                args:
                  ([0]): post-interrupt
                  ([1]): /root
                  (length(@)): 3
              - name: dax-post-interruptcheck
                args:
                  ([0]): post-interrupt-check
                  ([1]): /root
                  (length(@)): 3
            (tolerations[?key == 'nvidia.com/gpu']):
              - operator: Exists
    - assert:
        ## node complete, uncordoned
        resource:
          kind: Node
          apiVersion: v1
          metadata:
            labels:
              nodewright.nvidia.com/test-node: skyhooke2e
              nodewright.nvidia.com/status_interrupt-grouping: complete
            annotations:
              (length("nodewright.nvidia.com/version_interrupt-grouping") >= `6`): true
              nodewright.nvidia.com/nodeState_interrupt-grouping: >-
                {"dax|1.2.3":{"name":"dax","version":"1.2.3","image":"ghcr.io/nvidia/skyhook/agentless","stage":"post-interrupt","state":"complete"},"zeb|2.1.4":{"name":"zeb","version":"2.1.4","image":"ghcr.io/nvidia/skyhook/agentless","stage":"post-interrupt","state":"complete"}}
              nodewright.nvidia.com/status_interrupt-grouping: complete
            (!taints || length(taints)==`0` || (taints && !not_null(taints))): true ## taints should be empty or not exist
          status:
            (conditions[?type == 'nodewright.nvidia.com/interrupt-grouping/NotReady']):
            - reason: "Complete"
              status: "False"
            (conditions[?type == 'nodewright.nvidia.com/interrupt-grouping/Erroring']):
            - reason: "Not Erroring"
              status: "False"
    - assert:
        ## skyhook complete
        resource:
          kind: NodeWright
          apiVersion: nodewright.nvidia.com/v1alpha1
          metadata:
            name: interrupt-grouping
            annotations:
              ## test that version info is present
              ## the quotes are to escape the . and / in the key
              (length("nodewright.nvidia.com/version") >= `6`): true
          status:
            status: complete
            observedGeneration: 1
            nodeState:
              (values(@)):
                - dax|1.2.3:
                    name: dax
                    stage: post-interrupt
                    state: complete
                    version: 1.2.3
                    image: ghcr.io/nvidia/skyhook/agentless
                  zeb|2.1.4:
                    name: zeb
                    stage: post-interrupt
                    state: complete
                    version: 2.1.4
                    image: ghcr.io/nvidia/skyhook/agentless
            nodeStatus:
              (values(@)):
                - complete
    - assert:
        ## node-meta configmap
        resource:
          kind: ConfigMap
          apiVersion: v1
          metadata:
            namespace: nodewright
            labels:
              nodewright.nvidia.com/skyhook-node-meta: interrupt-grouping
            ownerReferences:
            - apiVersion: nodewright.nvidia.com/v1alpha1
              blockOwnerDeletion: true
              controller: true
              kind: NodeWright
              name: interrupt-grouping
          data:
            (length(@)): 3
            labels.json:
              (contains(@, 'nodewright.nvidia.com/test-node')): true
              (contains(@, 'nodewright.nvidia.com/status_interrupt-grouping')): true
            annotations.json:
              (contains(@, 'nodewright.nvidia.com/status_interrupt-grouping')): true
              (contains(@, 'nodewright.nvidia.com/nodeState_interrupt-grouping')): true
            packages.json:
              (contains(@, '"agentVersion"')): true
              (contains(@, '"dax"')): true
